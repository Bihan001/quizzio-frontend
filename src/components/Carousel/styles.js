import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  card: {
    width: '70vw',
    margin: '0 auto',
    borderRadius: '.5rem',
  },
  stepper: {
    margin: '0 auto',
    width: '60vw',
    backgroundColor: 'transparent',
  },
}));
